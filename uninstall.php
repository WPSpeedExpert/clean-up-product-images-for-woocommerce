<?php
// Exit if accessed directly
if (!defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

// Perform any necessary cleanup here. For this plugin, no settings or custom database tables are created,
// so no cleanup is needed.
